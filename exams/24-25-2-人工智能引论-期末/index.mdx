---
时间: 2024-2025学年第二学期
科目: 人工智能引论
阶段: 期末
类型: 本科
学院:
- 信息与通信工程学院
来源: fa40d71075330cefbd0b4460b6047028
答案完成度: 残缺
---

## 一、单选题（每空 1 分，共 20 分）

1. 人工智能的“能力”最本质的属性是什么？<Slot />

<Choices>
    - 精神
    - 意识
    + 学习、判断、理解等人类大脑的能力
    - 情感
</Choices>

2. 我国数学家吴文俊提出的“吴方法”主要应用于以下哪项？<Slot />

<Choices>
    - 语音识别
    + 数学定理机器证明
    - 图像识别
    - 机器人路径规划
</Choices>

3. 有关人工智能的智能函数的输入输出数据,以下说法不正确的是?<Slot />

<Choices>
    - 智能函数的各类输入数据，必须是计算机可理解的数学表达
    - 计算机看到的输入数据和人类看到的是不一样的
    - 不同类型的输入数据可以包含长度、宽度、时间等多维信息
    + 智能函数的输出形式和任务不一定相关
</Choices>

4. 机器学习的核心概念是？<Slot />

<Choices>
    - 手工编写规则
    + 系统通过数据自我学习和改进
    - 固定算法执行任务
    - 与物理学无关
</Choices>

5. 支持向量机（SVM）中，核函数的作用是？<Slot />

<Choices>
    - 降低特征维度
    + 将数据映射到高维空间
    - 优化求解速度
    - 防止过拟合
</Choices>

6. 以下哪种情况更适合使用朴素贝叶斯算法？<Slot />

<Choices>
    + 高维稀疏文本数据分类
    - 特征间存在强相关性的场景
    - 小样本复杂分类任务
    - 实时在线学习场景
</Choices>

7. DBSCAN 相较于 K-Means 的主要优势是什么？<Slot />

<Choices>
    - DBSCAN 聚类速度更快
    + DBSCAN 可以识别任意形状的簇并处理噪声
    - DBSCAN 不需要设置任何参数
    - DBSCAN 只适用于高维数据集
</Choices>

8. 神经网络的训练过程中，优化权重主要通过哪种核心方法实现？<Slot />

<Choices>
    - 使用遗传算法随机搜索最优权重组合
    + 通过反向传播计算损失函数的梯度并更新权重
    - 依靠预设规则直接分配固定权重值
    - 采用强化学习的奖励机制动态调整权重
</Choices>

9. 与全连接神经网络相比，卷积神经网络（CNN）的优势在于？<Slot />

<Choices>
    + 参数数量更少，可捕捉局部相关性
    - 无需训练即可工作
    - 只能处理一维数据
    - 计算速度更快
</Choices>

10. 下列哪项不是 CNN 的常见应用？<Slot />

<Choices>
    - 图像分类
    + 语音识别
    - 目标检测
    - 图像分割
</Choices>

11. 循环神经网络(RNN)相比前馈神经网络(FFNN)的主要优势是什么?<Slot />

<Choices>
    - 计算速度更快
    + 能处理序列数据的上下文依赖关系
    - 参数数量更少
    - 更适合图像识别任务
</Choices>

12. "看图说话"（Image Caption）任务中，通常如何结合 CNN 和循环神经网络（RNN）？<Slot />

<Choices>
    - CNN 生成文字，RNN 提取图像特征
    + CNN 作为图像编码器，RNN 作为文字解码器
    - RNN 处理图像，CNN 生成文字
    - CNN 和 RNN 并行处理数据
</Choices>

13. 相比于基础 RNN, 长短期记忆网络（LSTM）被更加广泛使用的主要原因是?<Slot />

<Choices>
    - 计算复杂度更低
    + 解决了长序列中的梯度消失问题
    - 不需要训练数据
    - 只能处理短序列
</Choices>

14. Transformer 模型的核心机制是什么？<Slot />

<Choices>
    - 卷积操作
    - 循环连接
    + 自注意力机制
    - 池化层
</Choices>

15. 强化学习的最大特点是？<Slot />

<Choices>
    - 依赖监督学习标签
    + 在交互中学习
    - 仅通过无监督学习聚类数据
    - 直接优化目标函数
</Choices>

16. 下列关于 Actor-Critic 模型描述错误的是?<Slot />

<Choices>
    - Critic 负责评估状态或状态-动作对的价值
    - Actor 负责学习和优化策略
    + Critic 和 Actor 可以独立学习，不需要相互协作
    - DDPG 是 Actor-Critic 模型的一种变体
</Choices>

17. 迁移学习中的“源域”指的是什么？<Slot />

<Choices>
    - 需要用有限数据解决的新任务
    + 拥有现有知识且通常有大量标记数据的领域
    - 用于知识迁移的特定算法
    - 用于训练模型的硬件
</Choices>

18. 大模型是指参数量巨大、训练数据规模庞大的机器学习模型，通常基于下列哪个技术？<Slot />

<Choices>
    - 知识图谱
    - 强化学习
    - 监督学习
    + 深度学习
</Choices>

19. 当进行自然语言任务中的文本聚类时，以下哪种距离度量方法在处理高维文本数据时较为常用？<Slot />

<Choices>
    - 欧式距离
    + 余弦相似度
    - Jaccard 距离
    - 以上都不是
</Choices>

20. 以下哪项是数智人与传统虚拟助手的核心功能差异？<Slot />

<Choices>
    - 仅能通过文字进行交互
    - 完全依赖预设脚本运行
    + 具备多模态交互和情境理解能力
    - 仅适用于单一设备平台
</Choices>

## 二、简答题（每题 5 分，共 40 分）

### 1.

简述机器学习中 “监督学习” 与 “无监督学习”的不同。

### 2.

简述分析 “分类” 与 “回归” 学习的不同。

### 3.

简述分析独热编码与分布式编码的不同。

### 4.

在计算机视觉领域 (CV) 领域, 基于参数的迁移学习可以将在 ImageNet 上预训练的模型参数迁移到目标任务——手写数字识别上, 请简述这个过程。

### 5.

请列举强化学习的四个基本组成要素，并简单说明它们的作用。

### 6.

列举业界知名的大模型？简述大模型的Transformer 架构及其工作原理？

### 7.

简述神经网络的梯度消失问题? 列举解决梯度消失问题的方法。

### 8.

什么是数字人（数智人）? 具有交互能力的数智人通常连接大模型,但大模型通常具有 “幻觉” 问题, 列举消除数智人幻觉的常用技术。

## 三、计算分析题（共 20 分）

### 1.

基于贝叶斯推断, 设计一个简单的广告邮件自动识别系统。假设在春节期间采集到了 10000 个邮件样本, 其中有 4000 封邮件被认定为广告邮件, 6000 封被认定为正常邮件。其中, 这 4000 封广告邮件中, 出现 “红包” 关键词的有 1000 封。而在 6000 封正常邮件中有 5000 封包含 “红包”这个词。现在, 某用户收到了一封出现 “红包” 关键词的邮件, 那么这封邮件是否是广告邮件还是正常邮件?

<Solution>
记 $A$ 为广告邮件，$B$ 为含”红包”关键词。

$$
P(A)=0.4,\quad P(\bar{A})=0.6
$$

$$
P(B|A)=\frac{1000}{4000}=0.25,\quad P(B|\bar{A})=\frac{5000}{6000}=\frac{5}{6}
$$

由全概率公式：

$$
P(B)=P(B|A)P(A)+P(B|\bar A)P(\bar A)=0.25\times0.4+\frac{5}{6}\times0.6=0.1+0.5=0.6
$$

由贝叶斯公式：

$$
P(A|B)=\frac{P(B|A)P(A)}{P(B)}=\frac{0.1}{0.6}=\frac{1}{6}\approx0.167
$$

$$
P(\bar A|B)=1-P(A|B)=\frac{5}{6}\approx0.833
$$

 $P(\bar A|B)>P(A|B)$，故该邮件判定为正常邮件。
</Solution>

### 2.

<Figure src="第三题图一.svg" float>
    第3.1题图
</Figure>

如图 1 所示的卷积神经网络。

1. 二维卷积计算。输入尺寸为 $4 \times 4$ 的图像，图像的像素值如图 2 所示，使用图 3 所示的 $2 \times 2$ 卷积核进行步长(Stride)为 1 的卷积运算,无填充(Padding=0)。请计算输出特征图的数值。
2. 池化计算。对上述卷积输出的 $3 \times 3$ 特征图进行 $2 \times 2$ 最大池化，步长为 2，无填充。计算最终输出尺寸和数值。

## 四、实践分析题（共 10 分）

<Figure src="题图4.svg" float>
    第4题图
</Figure>

在鸢尾花数据集上，使用 K-means 算法进行聚类，图A和B分别展示了不同 K 值下肘部法则和轮廓系数的图表。其中，肘部法则通过观察不同 K 值对应的总误差平方和（SSE）变化，选择 SSE 下降速度骤减的“拐点”作为最佳 K 值。轮廓系数综合衡量样本的簇内紧密度和簇间分离度，取值范围在 [-1, 1] 之间。值越接近 1，表示聚类效果越好。

1. 请描述 K-means 算法基本流程。
2. 请结合肘部法和轮廓系数综合比较分析，给出你所选择的最佳 K 值以及理由。

## 五、主观思考题（共 10 分）

论述人工智能技术的发展趋势及其对未来社会的潜在影响，并结合课程内容，探讨人工智能在教育、医疗、娱乐等不同领域的可能贡献与挑战。
